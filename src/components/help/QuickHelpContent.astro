---
import { useTranslations, type Lang } from '../../utils/i18n';
import { getRelativeLocaleUrl } from 'astro:i18n';

interface Props {
  lang: Lang;
}

const { lang } = Astro.props;
const t = useTranslations(lang);

// Get current locale from Astro
const currentLocale = Astro.currentLocale;
---

<div class="max-w-3xl mx-auto py-12 px-6">
  <div class="flex items-center justify-between gap-4">
    <h1 class="text-3xl font-bold mb-4">
      {t('title', { en: 'Quick Help', th: 'คู่มือด่วน' })}
    </h1>
    <a
      href={getRelativeLocaleUrl(currentLocale || 'en', 'home')}
      class="text-sm text-gray-600 underline hidden md:inline"
      >{t('homeLink', { en: 'Home', th: 'หน้าหลัก' })}</a
    >
  </div>

  <p class="text-gray-700 mb-6">
    {
      t('description', {
        en: "Here are the review buttons you'll see during study sessions and what they do.",
        th: 'นี่คือปุ่มต่าง ๆ ที่คุณจะเห็นระหว่างการทบทวน โดยแต่ละปุ่มมีหลักการทำงานดังนี้',
      })
    }
  </p>

  <!-- Front Side Buttons -->
  <section class="mb-8">
    <h2 class="text-2xl font-semibold mb-4 text-gray-800">
      {t('frontSideTitle', { en: 'Front Side', th: 'หน้าหน้าการ์ด' })}
    </h2>
    <p class="text-gray-600 mb-4 text-sm">
      {
        t('frontSideDescription', {
          en: 'When you see the question and need to decide how to proceed:',
          th: 'เมื่อคุณเห็นคำถามและต้องตัดสินใจว่าจะดำเนินการอย่างไร:',
        })
      }
    </p>

    <ul class="grid grid-cols-1 md:grid-cols-2 gap-4">
      <li class="p-4 rounded-lg border bg-white shadow-sm">
        <div class="flex items-start gap-3">
          <div class="text-3xl">👀</div>
          <div>
            <h3 class="font-semibold">
              {t('showMeTitle', { en: 'Show me', th: 'แสดงคำตอบ' })}
            </h3>
            <p class="text-sm text-gray-600">
              {
                t('showMeDescription', {
                  en: 'Reveal the translation or answer — use when you need a hint.',
                  th: 'เปิดดูคำแปลหรือคำตอบ — ใช้เมื่อคุณต้องการใบ้',
                })
              }
            </p>
          </div>
        </div>
      </li>

      <li class="p-4 rounded-lg border bg-white shadow-sm">
        <div class="flex items-start gap-3">
          <div class="text-3xl">💪</div>
          <div>
            <h3 class="font-semibold">
              {t('confidentTitle', { en: 'Confident', th: 'มั่นใจ' })}
            </h3>
            <p class="text-sm text-gray-600">
              {
                t('confidentDescription', {
                  en: 'You know this well — move to the next card immediately.',
                  th: 'คุณจำได้ดี — ข้ามไปการ์ดต่อไปเลย',
                })
              }
            </p>
          </div>
        </div>
      </li>
    </ul>
  </section>

  <!-- Back Side Buttons -->
  <section class="mb-8">
    <h2 class="text-2xl font-semibold mb-4 text-gray-800">
      {t('backSideTitle', { en: 'Back Side', th: 'หน้าหลังการ์ด' })}
    </h2>
    <p class="text-gray-600 mb-4 text-sm">
      {
        t('backSideDescription', {
          en: 'After seeing the answer, rate how well you remembered:',
          th: 'หลังจากเห็นคำตอบแล้ว ให้ประเมินว่าคุณจำได้ดีแค่ไหน:',
        })
      }
    </p>

    <ul class="grid grid-cols-1 md:grid-cols-3 gap-4">
      <li class="p-4 rounded-lg border bg-white shadow-sm">
        <div class="flex items-start gap-3">
          <div class="text-3xl">☝️</div>
          <div>
            <h3 class="font-semibold">
              {t('againTitle', { en: 'Again', th: 'อีกครั้ง' })}
            </h3>
            <p class="text-sm text-gray-600">
              {
                t('againDescription', {
                  en: 'You want to try this card again in the current review session.',
                  th: 'คุณต้องการทบทวนการ์ดใบนี้อีกครั้งในรอบนี้เลย',
                })
              }
            </p>
          </div>
        </div>
      </li>
      <li class="p-4 rounded-lg border bg-white shadow-sm">
        <div class="flex items-start gap-3">
          <div class="text-3xl">🙂</div>
          <div>
            <h3 class="font-semibold">
              {t('goodTitle', { en: 'Good', th: 'ดี' })}
            </h3>
            <p class="text-sm text-gray-600">
              {
                t('goodDescription', {
                  en: 'You remembered it but not perfectly — normal spacing applies.',
                  th: 'คุณจำได้แต่ไม่สมบูรณ์แบบ — จะใช้ระยะเวลาปกติในการทบทวน',
                })
              }
            </p>
          </div>
        </div>
      </li>
      <li class="p-4 rounded-lg border bg-white shadow-sm">
        <div class="flex items-start gap-3">
          <div class="text-3xl">✨</div>
          <div>
            <h3 class="font-semibold">
              {t('easyTitle', { en: 'Easy', th: 'ง่าย' })}
            </h3>
            <p class="text-sm text-gray-600">
              {
                t('easyDescription', {
                  en: 'You remembered it easily — the scheduler will space it longer.',
                  th: 'เห็นก็จำได้เลย — ระบบจะเว้นเวลากลับมาทบทวนให้นานขึ้น',
                })
              }
            </p>
          </div>
        </div>
      </li>
    </ul>
  </section>

  <div class="mt-8 text-sm text-gray-700 bg-gray-50 p-6 rounded-lg">
    <p class="mb-3 font-medium text-gray-800">
      {t('quickTipsTitle', { en: 'Quick tips:', th: 'เคล็ดลับสั้นๆ:' })}
    </p>
    <ul class="list-disc list-inside space-y-2">
      <li>
        {
          t('tip1', {
            en: "Choose Again or Good if you're not yet fluent, so the system will show you this card more frequently.",
            th: 'ให้เลือก Again หรือ Good เอาไว้ก่อน ถ้ายังคล่องเพื่อให้ระบบเอาการ์ดนี้มาให้เราดูบ่อยขึ้น',
          })
        }
      </li>
      <li>
        {
          t('tip2', {
            en: 'Think of this as memory practice, not a test of knowledge. This mindset makes reviewing more enjoyable.',
            th: 'ให้มองว่านี่เป็นการทบทวนความจำ เราไม่ได้มาสอบความรู้ ถ้ามองแบบนี้จะทำให้เราสนุกกับการทบทวนมากขึ้น',
          })
        }
      </li>
    </ul>
  </div>

  <div class="mt-8">
    <a
      href={getRelativeLocaleUrl(currentLocale || 'en', 'help')}
      class="text-primary-600 underline"
      >{
        t('helpLink', {
          en: 'Read more about how reviews are scheduled',
          th: 'อ่านเพิ่มเติมเกี่ยวกับการกำหนดเวลาทบทวน',
        })
      }</a
    >
  </div>
</div>
