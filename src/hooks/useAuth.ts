/**
 * useAuth Hook - Convenience Export
 *
 * Re-exports the useAuth hook for easier importing.
 * This follows the pattern of having dedicated hook files for better organization.
 */

export {
  useAuth,
  type AuthState,
  type AuthAction,
} from "../contexts/AuthContext";
